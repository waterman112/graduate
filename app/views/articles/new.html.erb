

<%= render :partial => "header" %>

<%= render :partial => "form" %>

<script>
	$("#form").validate({
		rules:{
      "article[title]":{
        "required": true,
      },
		},
		messages:{
			"article[title]":"请输入id",
		}
	});
</script>
